// Prisma Schema for AssetFlow
// Only generator + datasource — models will be added in subsequent phases.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}
